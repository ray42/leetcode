/*
EASY: https://leetcode.com/problems/image-smoother/

*/

#include <vector>

class Solution 
{
public:

    auto imageSmoother(const std::vector<std::vector<int>>& img) -> std::vector<std::vector<int>>
    {
        // We have to iterate through each cell, there is no way around it.
        //auto

        return {}; 
    }

private:

};

auto main(int argc, char* argv[]) -> int
{
    return 0;
}