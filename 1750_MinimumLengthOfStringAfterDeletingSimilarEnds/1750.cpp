#include <string>

class Solution 
{
public:
    auto minimumLength(const std::string& s) -> int
    {
        // Return the minimum length after removing similar ends
        // 1 <= s.length <= 105
        // s only consists of characters 'a', 'b', and 'c'.

        // Using two pointe, we 
        auto left = 0;
        auto right = s.size() - 1;

        //while(left < right)
        //{
        //    if
        //}

        return right - left;
    }
};


auto main(int argc, char* argv[]) -> int
{
    
    return 0;
}

